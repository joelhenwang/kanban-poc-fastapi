"""
Database package.

This package contains the SQLModel engine/session configuration and related helpers.

Having an `__init__.py` makes `app.db` a proper Python package, which improves
import reliability for tools like the FastAPI CLI and some IDEs.
"""

"""
Application data models package.

This module exists to make `app.models` a proper Python package for reliable
imports and better compatibility with tooling and the FastAPI CLI.

You can import model classes directly from their modules, e.g.:

- `from app.models.board import Board`
- `from app.models.task import Task`
- `from app.models.user import User`
"""
